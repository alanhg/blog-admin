import { AllowTabDirective } from './allow-tab.directive';

describe('AllowTabDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowTabDirective();
    expect(directive).toBeTruthy();
  });
});
